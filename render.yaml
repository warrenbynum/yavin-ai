# Render Blueprint - deploy with one click
services:
  - type: web
    name: yavin-ai
    runtime: docker
    repo: https://github.com/warrenbynum/yavin-ai
    plan: free
    healthCheckPath: /
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: yavin-db
          property: connectionString
      - key: SESSION_SECRET
        generateValue: true
      - key: PORT
        value: 8080
      - key: RUST_LOG
        value: info

databases:
  - name: yavin-db
    plan: free
    databaseName: yavin
